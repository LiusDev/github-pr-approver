# NOTE: This Docker Compose setup is superseded by Cloudflare Workers deployment.
# Use `pnpm deploy` (wrangler deploy) instead.
# This file is kept for reference only.

services:
    app:
        build:
            context: .
            network: host
        ports:
            - "8009:8070"
        volumes:
            - ./config.json:/app/config.json:ro
        restart: unless-stopped
